package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Colors & Core Theme Colors (Professional Polish Theme)
val NogramBlue = Color(0xFF0062A1)        // Major active primary color (#0062A1)
val NogramBlueDark = Color(0xFF001D36)    // Core text/on-container color (#001D36)
val NogramAlphaBlue = Color(0xFFD3E4FF)   // Material 3 Primary Container active blue (#D3E4FF)
val NogramBackgroundLight = Color(0xFFF7F9FB) // Screen canvas background (#F7F9FB)
val NogramSurfaceLight = Color(0xFFFFFFFF)    // Main surface card color (#FFFFFF)
val NogramTextPrimaryLight = Color(0xFF191C1E) // Heavy typography text (#191C1E)
val NogramTextSecondaryLight = Color(0xFF5C6F84) // Secondary subtext / Slate-gray

// Dark Colors (Mapped to modern light slate to maintain design consistency across system preferences)
val NogramBackgroundDark = Color(0xFFF7F9FB)       // Slate Gray background
val NogramSurfaceDark = Color(0xFFFFFFFF)           // Clean White card surfaces
val NogramSurfaceDarkVariant = Color(0xFFE6EBF2)    // Standard light gray background border
val NogramTextPrimaryDark = Color(0xFF191C1E)       // Dark Charcoal text (#191C1E)
val NogramTextSecondaryDark = Color(0xFF5C6F84)     // Cool steel gray text
val NogramGreenSuccess = Color(0xFF43A047)          // Success green

// Specialized Professional Polish backgrounds
val NogramPinnedBgDark = Color(0xFFF1F5F9)          // Light blueish tint for pinned chats
val NogramMessageMyDark = Color(0xFFD3E4FF)         // Outgoing chat bubbles (#D3E4FF)
val NogramMessageOtherDark = Color(0xFFFFFFFF)      // Incoming chat bubbles (#FFFFFF)

// Additional avatar palette from Design HTML
val AvatarRedBg = Color(0xFFF2B8B5)                 // Warm red/pink avatar background
val AvatarRedText = Color(0xFF601410)               // Warm red/pink avatar text
val AvatarGreenBg = Color(0xFFC1E8CF)               // Warm green avatar background
val AvatarGreenText = Color(0xFF06210E)             // Warm green avatar text
val AvatarBlueBg = Color(0xFFD3E4FF)                // Warm blue avatar background
val AvatarBlueText = Color(0xFF001D36)              // Warm blue avatar text
val AvatarGrayBg = Color(0xFFE2E8F0)                // Cool gray avatar background
val AvatarGrayText = Color(0xFF475569)              // Cool gray avatar text
